type Query {
    search(query: String!, threshold: Float = 0.8, limit: Int = 10, tags: [String], keywords: [String], contextKey: String, contextValue: String): [ContentChunk]
}

type ContentChunk {
    id: ID!
    chunkText: String
    sourceField: String
    sectionPath: String
}
